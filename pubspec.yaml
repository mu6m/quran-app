name: quran
description: "A new Flutter project."
publish_to: "none"

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/data/quran.json
    - lib/data/tafsir/ar-tafsir-ibn-kathir.json

  fonts:
    - family: HafsSmart_08
      fonts:
        - asset: lib/data/font/HafsSmart_08.ttf
